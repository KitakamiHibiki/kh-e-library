# kitakami_hibiki e-library 项目方案

> 版本：v0.0.3 · 文档日期：2026-08-03

---

## 一、项目概述

### 1.1 定位

自托管的个人电子书库，对标 Google Play 图书。支持 EPUB/PDF 格式的上传、管理、在线阅读，具备阅读进度同步等阅读辅助功能。最终部署形态为单个 Go 二进制文件，内嵌前端静态资源。

### 1.2 目标用户

个人使用，非多用户系统。无认证/鉴权，假定运行在可信网络环境。

### 1.3 当前完成度

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 0 | 项目脚手架、后端框架、数据库 schema | ✅ 完成 |
| Phase 1 | EPUB 上传解析、书架展示、基础阅读器 | ✅ 完成 |
| Phase 2 | 阅读进度同步 | ✅ 完成 |
| Phase 3 | 其他存储后端集成 | ❌ 未开始 |
| Phase 4 | 移动端适配、PWA | ❌ 未开始 |

---

## 二、技术栈

### 2.1 后端

| 组件 | 选型 | 版本 | 说明 |
|------|------|------|------|
| 语言 | Go | 1.25 | |
| HTTP 框架 | Gin | v1.12.0 | |
| ORM | GORM | v1.31.2 | |
| 数据库 | SQLite | v1.11.0 | 纯 Go 驱动 `glebarez/sqlite`，无需 CGO；启用 WAL 模式 + `busy_timeout=5000ms` |
| PDF 处理 | pdfcpu | v0.13.0 | 用于提取 PDF 首页图片作为封面 |
| 配置解析 | gopkg.in/yaml.v3 | v3.0.1 | |
| 静态资源 | Go embed.FS | — | 编译时将前端 dist 目录嵌入二进制 |

### 2.2 前端

| 组件 | 选型 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Vue 3 | ^3.5 | Composition API |
| 语言 | TypeScript | ~5.7 | |
| UI 库 | Element Plus | ^2.9 | |
| 路由 | Vue Router | ^4.5 | `createWebHistory` 模式 |
| 国际化 | vue-i18n | ^10 | legacy: false（Composition API 模式） |
| HTTP 客户端 | Axios | ^1.7 | |
| EPUB 阅读 | epubjs | ^0.3.93 | |
| PDF 阅读 | pdfjs-dist | ^4.0.379 | |
| 构建工具 | Vite | ^6.2 | |

### 2.3 部署

| 方式 | 说明 |
|------|------|
| 单二进制 | `CGO_ENABLED=0` 编译，跨平台无依赖 |
| Docker | `alpine:3.21` 基础镜像，多阶段构建或下载预编译二进制 |
| CI/CD | GitHub Actions，推送 `v*` tag 时自动构建 5 平台 + 发布 Release |

---

## 三、系统架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────┐
│              Web Browser                     │
│     书架 / 阅读器 / 设置 (Vue 3 SPA)         │
└──────────────────┬──────────────────────────┘
                   │ HTTP (/api/v1/*)
┌──────────────────▼──────────────────────────┐
│             Go Backend (Gin)                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐ │
│  │ Handler  │ │ Service  │ │  Repository  │ │
│  │  (Gin)   │→│ (业务层) │→│   (GORM)     │ │
│  └──────────┘ └──────────┘ └──────┬───────┘ │
│                                     │        │
│  ┌──────────────────────────────────┘        │
│  │ Storage Driver Interface                  │
│  │  ┌────────────┐  ┌──────────────────┐    │
│  │  │   Local    │  │  其他 (预留)       │    │
│  │  │ Filesystem │  │  Driver           │    │
│  │  └────────────┘  └──────────────────┘    │
│  └───────────────────────────────────────────│
│  ┌───────────────────────────────────────────│
│  │ embed.FS → 前端 SPA 静态资源服务           │
│  └───────────────────────────────────────────│
└──────────────────────────────────────────────┘
         │                    │
    ┌────▼─────┐       ┌─────▼──────┐
    │  SQLite   │       │ 本地文件系统  │
    │ library.db│       │ ./data/books│
    └──────────┘       └────────────┘
```

### 3.2 分层设计

```
Handler (HTTP 层)  →  Service (业务层)  →  Repository (数据层)  →  GORM / SQLite
                                            ↓
                                      Storage Driver (文件存储)
```

- **Handler**：解析 HTTP 请求参数，调用 Service，返回 JSON 响应
- **Service**：业务逻辑（校验、编排），不直接操作数据库
- **Repository**：GORM 数据访问，封装 CRUD
- **Storage Driver**：文件存储抽象，可插拔

### 3.3 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 单二进制部署 | Go embed.FS 嵌入前端 | 零依赖部署，用户体验最佳 |
| 纯 Go SQLite | `glebarez/sqlite` | 无需 CGO，支持交叉编译 |
| 存储驱动接口 | `storage.Driver` interface | 可插拔，支持本地/云端热切换 |
| API 参数风格 | Query String + 仅 GET/POST | 不使用 URL path 传参；禁止 PUT/DELETE；所有接口路径必须唯一 |
| 文件组织 | 按书籍 ID 分目录 | `data/books/{id}/book.epub`、`data/books/{id}/cover.jpg` |
| 数据库路径 | 仅存文件名 | `book.epub`、`cover.jpg`，不存绝对路径 |
| 静态资源 | API 代理 + 可选本地直连 | 统一通过 API 访问文件，兼容所有存储后端；本地存储可额外提供直连路径 |

---

## 四、数据模型

### 4.1 Book（书籍）

| 字段 | 类型 | JSON key | DB 约束 | 说明 |
|------|------|----------|---------|------|
| ID | uint | `id` | primaryKey | |
| Title | string | `title` | not null, index | 书名 |
| Author | string | `author` | index | 作者 |
| Publisher | string | `publisher` | — | 出版社 |
| ISBN | string | `isbn` | index | ISBN，仅当 `dc:identifier` 符合 ISBN-10/ISBN-13 格式时写入，否则为空字符串 |
| CoverFile | string | `cover` | — | 封面文件名，如 `cover.jpg`，空字符串表示无封面 |
| BookFile | string | `file` | not null | 书籍文件名，如 `book.epub`、`book.pdf` |
| FileType | string | `file_type` | not null | `"epub"` 或 `"pdf"` |
| FileSize | int64 | `file_size` | — | 文件大小（字节） |
| FileHash | string | `file_hash` | — | 文件 SHA-256 哈希值，用于重复检测 |
| Description | string | `description` | type:text | 简介 |
| Language | string | `language` | — | 语言 |
| Pages | int | `pages` | — | EPUB 为章节数（spine itemref 数量），PDF 为页数 |
| StorageKey | string | `storage_key` | default:"local" | 存储后端标识，见下方说明 |
| ReadStatus | string | `read_status` | default:"unread" | 阅读状态，见下方说明 |
| BookStatus | string | `book_status` | default:"processing" | 书本处理状态，见下方说明 |
| CreatedAt | int64 | `created_at` | — | 创建时间，秒级 Unix 时间戳（UTC） |
| UpdatedAt | int64 | `updated_at` | — | 更新时间，秒级 Unix 时间戳（UTC） |
| Tags | — | `tags` | `gorm:"-"` | 计算字段，通过关联表查询填充，不映射到数据库列 |

**StorageKey 说明：**
- 记录该书籍文件实际存储在哪个后端，取值对应 `storage.Driver` 的 `Name()` 返回值，当前仅 `"local"`
- 用途：当用户切换存储后端后，已有书籍仍记录在旧后端上，系统需根据 `StorageKey` 选择正确的 Driver 来访问文件，而非使用当前全局 Driver
- 上传时写入当前活跃的 Driver 名称，之后不变更（即使全局 Driver 切换，已有书籍仍从原后端读取）
- 未来扩展：支持跨后端迁移（如将 `StorageKey=local` 的书籍迁移到其他后端）

**ReadStatus 说明：**
- 取值：`"unread"`（未读）、`"reading"`（在读）、`"finished"`（已读完）
- 上传时默认为 `"unread"`
- 所有状态变更均由用户手动操作，后端不自动修改
- 前端应提供状态切换入口（如书架卡片上的状态下拉菜单）

**BookStatus 说明：**
- 取值：`"processing"`（处理中）、`"failed"`（处理失败）、`"ready"`（处理成功）、`"deleted"`（内部使用：并发删除时标记，见 5.3 节 delete 接口）
- 上传时默认为 `"processing"`，随后后端异步执行处理操作
- **处理操作**：从书本文件中提取元数据（封面、作者、出版社等），提取成功则更新为 `"ready"`，失败则更新为 `"failed"`
- 只有 `book_status = "ready"` 的书籍可以被阅读，`"failed"` 的书籍在前端显示处理失败并禁用操作
- `book_status = "processing"` 的书籍在前端显示处理中状态，不可操作

**异步处理保障机制：**
- **超时**：每个异步处理 goroutine 设置 5 分钟超时（`context.WithTimeout`），超时后更新 `book_status` 为 `"failed"`
- **崩溃恢复**：goroutine 内使用 `defer recover` 捕获 panic，捕获后更新 `book_status` 为 `"failed"`
- **启动修复**：服务启动时扫描所有 `book_status = "processing"` 的记录，将其重置为 `"failed"`（因为 processing 状态的 goroutine 已随上次进程终止而丢失）
- **并发删除**：异步处理 goroutine 使用数据库级检查避免 TOCTOU 竞态——执行 `UPDATE books SET book_status = 'ready' WHERE id = ? AND book_status = 'processing'`，检查 `RowsAffected > 0` 确认更新成功。若书籍已被删除则 `RowsAffected = 0`，静默退出

**设计说明：**
- `CoverFile` 和 `BookFile` 只存储**文件名**（如 `cover.jpg`、`book.epub`），不存储绝对路径
- 前端通过 `cover` 字段判空来判断有无封面（取代之前的 `has_cover` 计算字段）
- 前端通过 `file_type` 判断文件类型、阅读器模式
- 实际文件路径由后端根据 `{BooksDir}/{ID}/{filename}` 拼接，BooksDir 通过 `storage.local.books_dir` 设置配置
- `Tags` 字段为 `gorm:"-"` 计算字段，不存储在 books 表中，通过 BookTag 关联表 + Tag 表查询后填充
- JSON 序列化时 `tags` 返回标签名称数组，如 `["科幻", "经典"]`

### 4.2 文件存储结构

```
{BooksDir}/                          # 例如 ./data/books/
├── 1/                               # book_id = 1
│   ├── book.epub                    # 书籍文件（固定文件名前缀 book）
│   └── cover.jpg                    # 封面图片
├── 2/                               # book_id = 2
│   ├── book.pdf
│   └── cover.jpg
├── 3/
│   ├── book.epub
│   └── (无封面，无 cover 文件)
└── ...
```

**规则：**
- 每本书一个目录，目录名为书籍 ID
- 书籍文件名固定前缀 `book`，保留原始扩展名（`.epub` / `.pdf`）
- 封面文件名固定 `cover.{ext}`，扩展名保留原始格式（EPUB 封面可能是 `.jpg`/`.png`/`.gif` 等，保留原扩展名；PDF 封面统一为 `.jpg`，因为 `pdfcpu` 提取后转为 JPEG 编码）
- 删除书籍时，删除整个目录
- 无封面时，不创建 cover 文件，`CoverFile` 为空字符串

### 4.3 Tag（标签）

| 字段 | 类型 | JSON key | DB 约束 | 说明 |
|------|------|----------|---------|------|
| ID | uint | `id` | primaryKey | |
| Name | string | `name` | uniqueIndex, not null, size:64 | 标签名称，如 "科幻"、"经典" |

**设计说明：**
- Tag 表独立于 Book，标签全局唯一（`name` 唯一索引）
- 同一标签可被多本书使用，同一本书可有多个标签（多对多关系）
- 标签由用户手动添加或上传时自动提取（EPUB 的 `dc:subject`），不自动去重——由 `name` 唯一索引保证

### 4.4 BookTag（书籍-标签关联）

| 字段 | 类型 | DB 约束 | 说明 |
|------|------|---------|------|
| ID | uint | primaryKey | |
| BookID | uint | not null, index, FK→Book | |
| TagID | uint | not null, index, FK→Tag | |
| CreatedAt | int64 | — | 创建时间，秒级 Unix 时间戳（UTC） |

**唯一约束：** `(BookID, TagID)` 联合唯一索引，防止重复关联。

**设计说明：**
- 典型的多对多中间表
- 删除书籍时级联删除关联记录
- 删除标签时级联删除关联记录
- 查询书籍标签：`SELECT tag.name FROM book_tags JOIN tags ON book_tags.tag_id = tags.id WHERE book_tags.book_id = ?`

### 4.5 ReadingProgress（阅读进度）

| 字段 | 类型 | DB 约束 | 说明 |
|------|------|---------|------|
| ID | uint | primaryKey | |
| BookID | uint | not null, uniqueIndex, FK→Book | 每本书只有一条进度记录 |
| Progress | float64 | — | 0.0~1.0 进度百分比 |
| CFI | string | — | EPUB CFI 定位 / PDF 页码字符串。前端恢复进度时需根据书籍 `file_type` 决定如何使用该值：EPUB 调用 `rendition.display(cfi)`，PDF 调用 `pdfDoc.getPage(parseInt(cfi))` |
| ChapterHref | string | — | 当前章节 |
| CreatedAt | int64 | — | 创建时间，秒级 Unix 时间戳（UTC） |
| UpdatedAt | int64 | — | 更新时间，秒级 Unix 时间戳（UTC） |

### 4.6 Setting（运行时设置）

| 字段 | 类型 | DB 约束 | 说明 |
|------|------|---------|------|
| ID | uint | primaryKey | |
| Key | string | uniqueIndex, size:128, not null | |
| Value | string | type:text | |
| UpdatedAt | int64 | — | 更新时间，秒级 Unix 时间戳（UTC） |

**预定义设置项及默认值：**

| Key | 默认值 | 说明 |
|-----|--------|------|
| `ui.theme` | `"light"` | 界面主题：light / dark / auto |
| `ui.page_size` | `"20"` | 每页数量 |
| `ui.sort_field` | `"updated_at"` | 排序字段 |
| `ui.sort_order` | `"desc"` | 排序方式 |
| `reader.font_size` | `"16"` | 阅读字号 |
| `storage.driver` | `"local"` | 存储后端：当前仅 `"local"`，其他值在对应 Driver 实现后开放 |
| `storage.local.books_dir` | `"./data/books"` | 本地存储目录 |

**默认值机制：**
- 设置项不在数据库中预写入，默认值仅在代码中定义（常量/配置结构体）
- Repository 层提供 `GetWithDefault(key string) string` 方法：先查数据库，未找到则返回代码中的默认值
- 前端获取设置时（`GET /settings/list`），返回数据库值与默认值的合并结果，确保所有预定义 key 均有值返回
- 新版本新增设置项时无需数据库迁移，`GetWithDefault` 自动 fallback 到新默认值

---

## 五、API 接口

### 5.1 总览

所有接口前缀 `/api/v1`。

**接口规范：**
1. 不使用 URL path 传参，所有参数通过 query string 传递
2. 仅使用 GET 和 POST 两种请求方法，禁止 PUT、DELETE 等
3. 所有接口路径必须唯一，不允许不同方法使用相同路径
4. JSON 请求体最大 1MB（Gin 中间件 `MaxBodySize`），超出返回 413

**统一响应格式：**

```json
{
  "code": 200,
  "data": <payload>,
  "msg": "ok"
}
```

- `code`：业务状态码，与 HTTP 状态码一致（200 表示成功，4xx/5xx 表示失败）
- `data`：成功时为业务数据，失败时为 `null`
- `msg`：成功时为 `"ok"`，失败时为错误描述

错误响应示例：
```json
{
  "code": 404,
  "data": null,
  "msg": "书籍不存在"
}
```

**预定义错误码：**

| code | msg 示例 | 说明 |
|------|---------|------|
| 400 | "请求参数无效" | 参数缺失或格式错误 |
| 400 | "校验失败：ui.theme 仅接受 light/dark/auto" | 业务校验失败 |
| 403 | "书籍未处理完成，不可访问" | book_status 非 ready |
| 404 | "书籍不存在" | Book 未找到 |
| 404 | "封面不存在" | CoverFile 为空 |
| 404 | "标签不存在" | Tag 未找到 |
| 409 | "标签名称已存在" | Tag name 唯一约束冲突 |
| 409 | "书籍-标签关联已存在" | BookTag 联合唯一约束冲突 |
| 413 | "文件超过大小限制" | 上传文件过大 |
| 500 | "服务器内部错误" | 未预期错误 |

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/books/list` | 书籍列表（分页+搜索） |
| GET | `/books/detail?id=` | 获取单本书详情 |
| POST | `/books/create` | 上传书籍（multipart/form-data） |
| POST | `/books/update?id=` | 更新书籍元数据 |
| POST | `/books/reprocess?id=` | 重新处理书籍（仅限 book_status=failed） |
| POST | `/books/delete?id=` | 删除书籍 |
| POST | `/books/batch_delete` | 批量删除书籍（JSON body） |
| GET | `/books/read?id=` | 读取书籍文件（在线阅读器渲染） |
| GET | `/books/download?id=` | 下载书籍文件（浏览器另存为） |
| GET | `/books/cover?id=` | 获取封面图片 |
| GET | `/books/progress?id=` | 获取阅读进度 |
| POST | `/books/progress/save` | 保存阅读进度（JSON body） |
| GET | `/tags/list` | 获取所有标签列表 |
| POST | `/tags/create` | 创建标签 |
| POST | `/tags/update?id=` | 重命名标签 |
| POST | `/tags/delete?id=` | 删除标签 |
| POST | `/books/tags/add` | 为书籍添加标签（关联） |
| POST | `/books/tags/remove` | 移除书籍的某个标签（JSON body） |
| GET | `/settings/list` | 获取所有设置 |
| POST | `/settings/update` | 批量更新设置 |
| GET | `/health` | 健康检查（Docker/负载均衡用） |
| GET | `/stats/overview` | 阅读统计概览 |

### 5.2 文件访问接口

书籍文件和封面通过 API 接口访问（路由见 5.1 总览表），后端根据书籍的 `StorageKey` 选择对应的 Driver 读取文件并流式返回：

**设计说明：**
- 本地存储（`StorageKey=local`）：后端从 `{BooksDir}/{id}/book.{ext}` 读取文件，通过 `c.DataFromReader` 流式返回
- 其他存储后端：后端有两种策略：
  - **代理模式**：后端从远端下载文件到临时缓冲，再流式返回给前端。适合小文件，但大文件会占用后端内存/带宽
  - **重定向模式**：后端调用 Driver 生成临时下载 URL，返回 `302 Redirect`。前端直接从远端下载，不经过后端，适合大文件
- 具体采用哪种策略由 Driver 的 `GetURL` 方法决定：返回 `(url, ephemeral=true, nil)` 时后端做 302 重定向；返回 `(url, ephemeral=false, nil)` 时后端代理返回文件内容
- 前端统一使用 `/books/read?id=` 和 `/books/cover?id=`，无需关心底层存储方式

**本地存储的额外优化：**
- 对于本地存储的书籍，后端在启动时额外注册 Gin 静态路由 `r.Static("/files/books", booksDir)`，提供直接的文件系统访问
- 前端可通过 `/files/books/{id}/book.{ext}` 直接访问本地文件，绕过 Go Handler，性能更好
- 该路径仅在本地存储模式下可用，前端不应硬性依赖此路径
- **当前状态**：前端阅读器统一使用 `/api/v1/books/read?id=` API 路径，未使用此直连优化。直连路径作为性能优化预留，后续可按需启用（如大文件场景）
- **安全措施**：使用自定义 `http.FileSystem` 包装，限制只能访问 `{id}/book.{ext}` 和 `{id}/cover.{ext}` 格式的路径，拒绝包含 `..` 的路径，不跟随符号链接

### 5.3 接口详情

#### GET /books/list

**Query 参数：**
- `page` (int, 默认 1)
- `page_size` (int, 默认 20, 上限 100)
- `keyword` (string, 可选，搜索 title/author)
- `tag` (string, 可选，按标签名称筛选，仅返回包含该标签的书籍)
- `book_status` (string, 可选，按 `book_status` 筛选，取值 `"processing"` / `"ready"` / `"failed"`)

**响应：**
```json
{
  "code": 200,
  "data": {
    "list": [Book, ...],
    "total": 42,
    "page": 1
  },
  "msg": "ok"
}
```

#### GET /books/detail?id=1

**响应：**
```json
{
  "data": {
    "id": 1,
    "title": "书名",
    "author": "作者",
    "cover": "cover.jpg",
    "file": "book.epub",
    "file_type": "epub",
    "file_size": 1048576,
    "file_hash": "abc123...",
    "tags": ["科幻", "经典"],
    ...
  }
}
```

说明：`cover` 和 `file` 为文件名（用于展示），文件内容通过 API 接口获取：
- 封面：`GET /books/cover?id=1`
- 书籍：`GET /books/read?id=1`
- 无封面时 `cover` 为空字符串，前端不渲染封面图

#### POST /books/create

**请求：** `multipart/form-data`，字段 `file` 为 EPUB/PDF 文件，最大 200MB，超出返回 413。

**工作流程：**
1. 校验文件后缀（仅 `.epub` / `.pdf`，**不区分大小写**，如 `.EPUB` / `.Epub` 均接受）
2. 计算文件 SHA-256 哈希值，若与已有书籍的 `FileHash` 重复，返回 409 错误（"文件已存在"），阻止重复上传
3. 基础文件结构校验：
   - EPUB：验证 ZIP 内存在 `mimetype` 文件且内容为 `application/epub+zip`，存在 `META-INF/container.xml`；校验不通过则返回 400 错误，不创建记录
   - PDF：验证文件头以 `%PDF-` 开头；校验不通过则返回 400 错误，不创建记录
4. 创建数据库记录（title 取文件名，`file_type` 统一为小写 `"epub"` / `"pdf"`，`book_status` 设为 `"processing"`，写入 `file_hash`），获取生成的 Book ID
5. 创建目录 `{BooksDir}/{id}/`，将上传文件保存为 `book.{ext}`（`ext` 统一为小写），同时写入 `file_size`（从 `Save` 返回的 `size` 获取）
6. 立即返回 `201 Created`，响应中包含 Book 对象（`book_status: "processing"`）
7. **异步**执行处理操作（goroutine，含超时/崩溃恢复，见 4.1 BookStatus 说明）：
   - EPUB：解析 OPF 提取 title/author/publisher/ISBN/language/description/pages，提取封面图片保存为 `cover.{ext}`；若 `dc:subject` 存在，自动创建标签并关联
   - PDF：使用 pdfcpu 提取首页图片保存为 `cover.jpg`
   - 处理成功：更新 `book_status` 为 `"ready"`
   - 处理失败：更新 `book_status` 为 `"failed"`

**说明：** 前端应在书架页对 `"processing"` 状态的书籍显示处理中标识，对 `"failed"` 状态的书籍显示处理失败并禁用阅读操作。前端可通过轮询 `GET /books/detail?id=` 检查 `book_status` 变化，建议轮询间隔 2 秒，状态变为 `"ready"` 或 `"failed"` 后停止。

**上传失败回滚：** 若步骤 5（文件保存）失败，应回滚步骤 4（删除已创建的数据库记录），返回 500 错误，确保数据库与文件系统状态一致。

#### POST /books/update?id=1

更新书籍元数据（标题、作者、阅读状态等）。

**请求体：** JSON，仅接受以下字段（未传的字段不更新）：
- `title` (string) — 书名
- `author` (string) — 作者
- `publisher` (string) — 出版社
- `read_status` (string) — 阅读状态，仅接受 `"unread"` / `"reading"` / `"finished"`

**实现方式：** Handler 解析 JSON 到 `map[string]interface{}`，仅将请求中实际包含的字段传入 `DB.Model(&book).Updates(map)`，确保未传字段不被零值覆盖。禁止使用 `DB.Save(&book)` 全量保存。

不接受 `file_type`、`file_size`、`storage_key`、`cover`、`file`、`book_status` 等字段的修改。标签的增删通过独立的 `/books/tags/add` 和 `/books/tags/remove` 接口操作。重新处理书籍通过独立的 `/books/reprocess` 接口（见下方）。

#### POST /books/reprocess?id=1

对 `book_status = "failed"` 的书籍触发重新处理。仅限 `"failed"` 状态，其他状态返回错误。

**行为：**
1. 校验 `book_status` 为 `"failed"`，否则返回 400
2. 删除已有的封面文件（若 `CoverFile` 非空，调用 Driver 删除）
3. 清空 `CoverFile` 字段
4. 更新 `book_status` 为 `"processing"`
5. 启动新的异步处理 goroutine（与上传时的异步处理逻辑相同，含超时/崩溃恢复）

#### POST /books/delete?id=1

删除书籍及其关联的所有文件和数据。

**行为：**
1. 检查 `book_status`：
   - 若为 `"processing"`：通过原子 UPDATE `UPDATE books SET book_status = 'deleted' WHERE id = ? AND book_status = 'processing'` 标记删除。若 `RowsAffected = 0` 说明状态已变更（如异步 goroutine 刚刚完成处理），回退到正常删除流程。该步骤的目的是通知仍在运行的异步 goroutine 放弃操作——goroutine 处理完成后执行自身 UPDATE 时会因 `WHERE book_status = 'processing'` 条件不满足而发现 `RowsAffected = 0`，静默退出（并发删除机制见 4.1 节）
   - 若为非 `"processing"`：跳过步骤 1，直接进入步骤 2
2. 根据书籍 `StorageKey` 查找对应 Driver，调用 `DeleteBook(bookID)` 删除书籍所在目录（包含书籍文件和封面）
3. 删除数据库中的 Book 记录（若步骤 1 已将其状态改为 `"deleted"`，此处执行 DELETE；否则直接 DELETE）
4. 级联删除关联数据：ReadingProgress、BookTag 关联记录

#### POST /books/batch_delete

批量删除书籍及其关联的所有文件和数据。

**请求体：**
```json
{
  "ids": [1, 2, 3]
}
```

**行为：** 对每个 id 依次执行与单个删除相同的操作（删除目录、数据库记录、级联关联数据）。任一 id 不存在时跳过该 id，不中断整个操作。返回实际删除的数量。

**响应：**
```json
{
  "data": {
    "deleted": 2
  }
}
```

#### GET /books/read?id=1

流式返回书籍文件内容，用于在线阅读器（epubjs / pdfjs）渲染。仅 `book_status = "ready"` 的书籍可访问，其他状态返回 403。

**行为：**
- 后端根据书籍的 `StorageKey` 从注册表查找对应 Driver
- 本地存储：读取 `{BooksDir}/{id}/book.{ext}`，通过 `c.DataFromReader` 流式返回，Content-Type 根据 FileType 设置（`application/epub+zip` 或 `application/pdf`）
- 支持 HTTP Range 请求（`Accept-Ranges: bytes`），解析 `Range` 头返回指定字节范围（206 Partial Content），使 epubjs 可以按需加载章节
- 其他存储后端：调用 Driver 的 `GetURL` 方法，若返回临时 URL 则 302 重定向，否则代理下载后返回

#### GET /books/download?id=1

触发浏览器下载书籍文件（另存为）。仅 `book_status = "ready"` 的书籍可下载。

**行为：**
- 设置 `Content-Disposition: attachment; filename="{title}.{ext}"`，文件名取 `Book.Title + FileType 扩展名`
- Content-Type 统一为 `application/octet-stream`，避免浏览器尝试预览
- **禁用 Range 请求**：下载模式返回完整文件（200 OK），不响应 `Range` 头，确保浏览器下载完整文件而非部分内容
- 本地存储：从 `{BooksDir}/{id}/book.{ext}` 读取文件流式返回
- 其他存储后端：优先 302 重定向到临时 URL；若不支持临时 URL 则代理下载

#### GET /books/cover?id=1

返回封面图片文件。

**行为：**
- 与 Read 接口逻辑相同，读取 `{id}/cover.{ext}` 文件
- 无封面时返回 404

#### POST /books/progress/save

**请求体：**
```json
{
  "book_id": 1,
  "progress": 0.35,
  "cfi": "epubcfi(/6/4!/4/2/1:0)",
  "chapter_href": "chapter1.xhtml"
}
```

Upsert 逻辑：同一 book_id 只保留一条记录。使用 SQLite 的 `INSERT ... ON CONFLICT(book_id) DO UPDATE SET ...` 语法（UPSERT，SQLite 3.24+），保留 `CreatedAt` 字段不被重置。避免 `INSERT OR REPLACE`（会 DELETE+INSERT 导致 CreatedAt 被覆盖）和 `FirstOrCreate` + `Save`（有竞态条件）。

#### GET /tags/list

**Query 参数：**
- `keyword` (string, 可选，按标签名称搜索)

**响应：**
```json
{
  "data": [
    {"id": 1, "name": "科幻", "count": 5},
    {"id": 2, "name": "经典", "count": 3}
  ]
}
```

`count` 为该标签关联的书籍数量。标签数量通常不多，暂不分页。

#### POST /tags/create

**请求体：**
```json
{
  "name": "科幻"
}
```

创建标签，`name` 必须唯一（唯一索引约束），重复时返回错误。

#### POST /tags/update?id=1

**请求体：**
```json
{
  "name": "新标签名"
}
```

重命名标签，关联关系（BookTag）保持不变。新名称与已有标签重复时返回错误。

#### POST /tags/delete?id=1

删除标签及其所有 BookTag 关联记录。若该标签下无任何关联书籍，则仅删除标签本身。

#### POST /books/tags/add

**请求体：**
```json
{
  "book_id": 1,
  "tag_id": 2
}
```

为书籍添加标签关联，`(BookID, TagID)` 联合唯一约束防止重复关联。

#### POST /books/tags/remove

**请求体：**
```json
{
  "book_id": 1,
  "tag_id": 2
}
```

移除书籍的某个标签关联，与 `tags/add` 使用相同的 JSON body 格式。

#### GET /settings/list

返回所有预定义设置项的当前值（数据库值与默认值合并，确保所有 key 均有值返回）。

**说明：** 设置项数量有限（当前 7 个预定义项），暂不分页。未来新增 storage driver 配置项后数量仍可控，若超过 50 项再考虑分页。

#### POST /settings/update

**请求体：**
```json
{
  "settings": {
    "ui.theme": "dark",
    "storage.driver": "local"
  }
}
```

批量 upsert，保存后触发 `onSettings` 回调，热重载 Storage Driver。

**校验规则：**
- key 必须为预定义设置项之一（见 4.6 节），未知 key 返回错误
- `ui.theme`：仅接受 `"light"` / `"dark"` / `"auto"`
- `ui.page_size`：5~100 的整数字符串
- `ui.sort_field`：仅接受 `"updated_at"` / `"title"` / `"author"` / `"created_at"`
- `ui.sort_order`：仅接受 `"asc"` / `"desc"`
- `reader.font_size`：12~32 的整数字符串
- `storage.driver`：仅接受 `"local"`（当前仅实现了 LocalDriver，其他值在对应 Driver 实现后开放）
- `storage.local.books_dir`：非空字符串
- `storage.{driver_name}.*`：对应存储后端的配置项，在 Driver 实现时定义

#### GET /health

健康检查接口，不经过 `/api/v1` 前缀，直接挂载在根路径。

**响应：**
```json
{
  "code": 200,
  "data": {"status": "ok"},
  "msg": "ok"
}
```

检查项：SQLite 连接可用。任一检查失败返回 503。

#### GET /stats/overview

返回阅读统计概览。

**响应：**
```json
{
  "code": 200,
  "data": {
    "total_books": 42,
    "unread_count": 10,
    "reading_count": 5,
    "finished_count": 27,
    "recent_reading": [
      {"id": 1, "title": "书名", "updated_at": 1722758400}
    ]
  },
  "msg": "ok"
}
```

- `total_books`：总藏书量（所有 book_status=ready 的书籍）
- `unread_count` / `reading_count` / `finished_count`：按 ReadStatus 统计
- `recent_reading`：最近阅读的 5 本书（按 progress.updated_at 降序）

---

## 六、存储后端

### 6.1 Driver 接口

```go
type Driver interface {
    Name() string
    Save(bookID uint, filename string, reader io.Reader) (fileName string, size int64, err error)
    Open(bookID uint, fileName string) (io.ReadCloser, error)
    DeleteBook(bookID uint) error
    Exists(bookID uint, fileName string) (bool, error)
    GetURL(bookID uint, fileName string) (url string, ephemeral bool, err error)
    Stat(bookID uint, fileName string) (*FileInfo, error)
}
```

说明：所有方法以 `bookID` 为第一参数，由 Driver 内部拼接 `{BooksDir}/{bookID}/{fileName}` 完整路径。上层业务代码只需传递 ID 和文件名。

**安全要求：** Driver 实现中必须对 `filename` 参数做 `filepath.Base()` 清洗，去除路径分隔符，防止路径遍历攻击（如 `../../etc/passwd`）。清洗后再校验结果：不包含 `\`、`/`、`:`，不以 `.` 开头，不为空。不满足任一条件则拒绝操作。

### 6.2 LocalDriver（已实现）

- `Save`：按书籍 ID 创建子目录，保存文件
  - 调用方式：`Save(bookID uint, filename string, reader io.Reader) (fileName string, size int64, err error)`
  - 创建 `{BooksDir}/{bookID}/` 目录
  - 书籍文件保存为 `book.{ext}`（ext 保留原始扩展名）
  - 封面文件保存为 `cover.{ext}`
  - 返回文件名（如 `book.epub`），而非绝对路径
- `Open`：`os.Open(filepath.Join(d.BooksDir, strconv.Itoa(bookID), fileName))`
- `Delete`：删除整个 `{BooksDir}/{bookID}/` 目录（`os.RemoveAll`）
- `GetURL`：返回相对于 BooksDir 的路径（如 `1/book.epub`）
- 路径解析：优先使用工作目录（`os.Getwd()`），回退到可执行文件目录

### 6.3 其他存储后端（预留）

当前仅实现 LocalDriver。Driver 接口设计为可插拔，未来可扩展其他存储后端（如百度网盘、S3 等），只需实现 `storage.Driver` 接口并在 `storage.Factory` 中注册即可，无需修改业务层代码。

具体存储后端的实现细节（如 OAuth 授权流程、API 调用方式等）留待对应阶段再设计。设置页中 `storage.driver` 选项当前仅提供 `"local"`，其他选项在对应 Driver 实现后开放。

### 6.4 热切换

设置页面修改 `storage.driver` 后，调用 `POST /settings/update`，后端触发 `onSettings` 回调，重新初始化对应的 Driver 实例并替换 Service 中的引用。

**Driver 注册表设计：**
- Service 层维护一个 Driver 注册表 `map[string]storage.Driver`，而非单个 Driver 引用
- 初始化时注册所有已配置的 Driver（当前仅 `"local"`）
- 热切换时：初始化新 Driver 并注册到注册表，**不删除旧 Driver**（已有书籍仍需通过旧 Driver 访问）
- 访问书籍文件时，根据 `book.StorageKey` 从注册表查找对应 Driver，而非使用全局活跃 Driver
- 示例：用户从 `"local"` 切换到其他后端后，`StorageKey=local` 的书籍仍通过 LocalDriver 访问，新上传的书籍使用新 Driver

**热切换失败回滚：**
- `onSettings` 回调中，先初始化新 Driver，初始化成功后再注册到注册表
- 若新 Driver 初始化失败（如凭证无效、网络不可达），回滚设置：将 `storage.driver` 恢复为旧值，返回错误响应
- 设置更新接口应在 `onSettings` 回调失败时回滚数据库中的设置变更，流程如下：
  1. **保存旧值**：Handler 在事务提交前，从数据库读取本次被更新的所有 key 的当前值（即旧值），存入 `map[string]string`
  2. **验证 + 写入**：校验新值（见 5.3 节校验规则），通过后在事务中将新值写入数据库，提交事务
  3. **回调**：事务外调用 `onSettings`，传入本次变更的 key-value 集合
  4. **失败回滚**：若 `onSettings` 返回 error，使用步骤 1 保存的旧值发起回滚写入（`UPDATE settings SET value = oldValue WHERE key = k`），将改动过的 key 逐一恢复为旧值，返回错误响应。回滚写入应使用独立事务
- 避免在事务内执行回调（事务内 IO 会长时间占用 SQLite 写锁）。

**`onSettings` 回调范围：**
- `storage.driver` 变更 → 初始化新 Driver 并注册到注册表
- `storage.local.books_dir` 变更 → 重新初始化 LocalDriver（更新 `BooksDir` 路径），替换注册表中的 `"local"` Driver。**注意**：修改 `books_dir` 后，已有书籍的文件仍在旧目录中，需用户手动迁移文件到新目录，系统不会自动迁移。
- 其他设置项（`ui.*`、`reader.*`）→ 无需后端即时生效，前端下次读取时自动应用

---

## 七、前端设计

### 7.1 页面结构

| 路由 | 组件 | 加载方式 | 说明 |
|------|------|---------|------|
| `/` | BookShelf.vue | 同步 | 书架首页 |
| `/read?id=` | Reader.vue | 异步 | 阅读器 |
| `/settings` | Settings.vue | 异步 | 设置页 |

### 7.2 书架页（BookShelf.vue）

**功能：**
- 顶部：标题 + 搜索框 + 上传按钮 + 设置按钮
- 主体：响应式网格（`el-row`/`el-col`，xs=12, sm=8, md=6, lg=4）
- 每张卡片：封面图（`book.cover` 非空时通过 `GET /books/cover?id=` 加载） / PDF 标记 / 占位符 + 标题 + 作者 + 阅读/删除按钮
- 底部分页（`el-pagination`，每页 20 条）
- 上传：`el-upload` 自定义 `http-request`，仅接受 `.epub`/`.pdf`
- 删除：`ElMessageBox.confirm` 确认后调用 API
- 空状态：`el-empty` 组件

**已知问题：**
- `pageSize` 硬编码为 20，未使用设置中的 `ui.page_size`
- 排序未使用设置中的 `ui.sort_field` / `ui.sort_order`
- 搜索仅触发一次，无防抖

### 7.3 阅读器页（Reader.vue）

**EPUB 阅读器：**
- 使用 `epubjs`（`singleFile: true` 模式）加载并渲染到 `#reader-area` div
- 书籍 URL：`/api/v1/books/read?id=${bookId}`
- 后端需支持 HTTP Range 请求（`Accept-Ranges: bytes`），使 epubjs 可以按需加载特定章节而非整文件下载。若浏览器不支持或超过 2 分钟未收到 response，自动回退到全文件加载模式
- 目录导航：通过 `book.loaded.navigation` 获取 EPUB 目录（toc），渲染为侧边栏导航，点击章节跳转到对应 CFI
- 监听 `relocated` 事件追踪进度，2 秒防抖自动保存
- 恢复上次 CFI 位置（断点续读）：打开书籍时调用 `GET /books/progress?id=` 获取已保存的进度（包含 `cfi` 和 `progress` 字段），根据书籍的 `file_type` 恢复阅读位置——EPUB 调用 `rendition.display(cfi)`，PDF 调用 `pdfDoc.getPage(parseInt(cfi))` 跳转到对应页码

**PDF 阅读器：**
- 使用 `pdfjs-dist` 加载，单页渲染到 `<canvas>`
- 书籍 URL：`/api/v1/books/read?id=${bookId}`
- 翻页：上一页/下一页按钮 + 页码输入框
- 缩放：+/- 按钮（25%~300%），适应高度按钮，Ctrl+/-/0 快捷键
- 进度：2 秒防抖自动保存，CFI 字段存储页码字符串
- 首页渲染：先 `loading = false` + `await nextTick()` 确保 canvas 在 DOM 中

**共享功能：**
- 顶部工具栏：返回按钮、书名、进度百分比
- `watch(route.query.id)` 监听路由变化，切换书籍时重新初始化
- `cleanup()` 函数：销毁 rendition/book/pdfDoc，重置所有状态
- `onBeforeUnmount` 调用 `cleanup()` 释放资源

**已知问题：**
- PDF 每次只渲染一页，无法滚动浏览
- EPUB 字号未使用设置中的 `reader.font_size`
- 无主题切换（设置中的 `ui.theme` 未生效）
- pdfjs worker 从 CDN 加载，离线环境不可用

### 7.4 设置页（Settings.vue）

**表单字段：**
- 界面语言：zh-CN / en（保存到 localStorage，立即生效）
- 界面主题：浅色/深色/跟随系统（**未实际生效**）
- 排序字段：更新时间/书名/作者/创建时间（**未实际生效**）
- 排序方式：降序/升序（**未实际生效**）
- 每页数量：5-100（**未实际生效**）
- 阅读字号：12-32 滑块（**未实际生效**）
- 存储后端：本地文件系统（其他后端在实现后开放）
- 本地存储目录

**样式：** 表单最大宽度 35rem，使用 rem 相对单位。

**已知问题：**
- 多数设置项仅保存到数据库，前端未读取并应用
- 主题切换未实现
- 其他存储后端选项暂无实际功能

### 7.5 国际化

- 使用 `vue-i18n` Composition API 模式（`legacy: false`）
- 支持简体中文（默认）和英文
- 语言偏好保存到 `localStorage`（key: `kh-e-library-locale`）
- Element Plus locale 与 vue-i18n 同步切换
- 语言包结构：`bookShelf.*` / `reader.*` / `settings.*`（嵌套对象）

### 7.6 API 客户端

- Axios 实例，baseURL: `/api/v1`
- 开发模式：Vite 代理 `/api` → `http://localhost:14325`
- 生产模式：前端嵌入 Go 二进制，同源请求
- 书籍文件和封面统一通过 API 接口访问：
  - `getReadUrl(id)` → `/api/v1/books/read?id=${id}`
  - `getCoverUrl(id)` → `/api/v1/books/cover?id=${id}`

---

## 八、EPUB 解析

### 8.1 解析流程

1. 读取 ZIP 文件中的 `META-INF/container.xml`，获取 OPF 文件路径
2. 解析 OPF 文件（Dublin Core 命名空间）：
   - `dc:title` → Title
   - `dc:creator` → Author（多个 creator 用 `", "` 分隔拼接，如 `"作者1, 作者2"`）
   - `dc:publisher` → Publisher
   - `dc:identifier` → ISBN（仅当值符合 ISBN-10 或 ISBN-13 格式时写入，否则忽略）
   - `dc:language` → Language
   - `dc:description` → Description
   - `dc:subject` → Tags（多个 subject 元素对应多个标签，自动创建 Tag 记录并关联）
   - `<spine><itemref>` → Spine（章节数）
3. 封面提取：
   - 优先查找 `<item properties="cover-image">`
   - 回退查找 `<meta name="cover">` → 对应 manifest item
   - 提取封面图片后根据**实际文件内容**（magic bytes）确定扩展名：JPEG（`\xFF\xD8\xFF`）→ `.jpg`，PNG（`\x89PNG`）→ `.png`，GIF（`GIF8`）→ `.gif` 等。不依赖 manifest 的 `media-type` 或文件扩展名，因为两者可能与实际内容不一致
   - 封面图片压缩：若提取的封面超过 1MB，自动缩放/压缩至 1MB 以内（可用 Go `image` 标准库 + `image/jpeg` 编码）

### 8.2 PDF 处理

**元数据提取：**
- 使用 `pdfcpu` 读取 PDF 信息字典（`InfoDict`），提取以下字段：
  - `Title` → Book.Title（若为空则回退到文件名）
  - `Author` → Book.Author
  - `Subject` → Book.Description
  - `Creator`/`Producer` → 忽略（为 PDF 制作工具，非书籍作者/出版社）
- PDF 无标准 ISBN 字段，`Book.ISBN` 留空
- PDF 无标准章节数，`Book.Pages` 从 `PageCount` 获取

**封面提取：**
- 使用 `pdfcpu` 的 `ExtractImages` 提取第 1 页的图片，保存为 `cover.jpg`
- 若首页无嵌入图片（纯矢量/文字页面），则 `CoverFile` 留空，不生成封面文件

---

## 九、前端静态资源服务

### 9.1 embed.FS

```go
//go:embed dist
var frontendFS embed.FS
```

编译时将 `client/web/dist` 目录嵌入 Go 二进制。

### 9.2 SPA Fallback

- `/api/*` 路径：直接透传给 Gin API Handler
- 其他路径：尝试从 embed.FS 返回精确匹配的静态文件
- 静态文件不存在时：返回 `index.html`（SPA 客户端路由）

---

## 十、配置与部署

### 10.0 优雅关闭

- 监听 `SIGINT` / `SIGTERM` 信号
- 收到信号后：停止接受新请求和新上传（返回 503），使用 `sync.WaitGroup` 等待所有活跃的异步处理 goroutine 完成或超时（最长 5 分钟，与单个 goroutine 超时一致）
- 等待进行中的 HTTP 请求完成（最长 10 秒超时）
- 超时后强制关闭，关闭 SQLite 连接
- 启动时扫描 `book_status = "processing"` 的记录并重置为 `"failed"`（弥补关闭时未完成的异步处理）
- 确保上传流程的原子性：文件保存与数据库记录在同一流程中完成，避免关闭时出现数据库有记录但文件未保存的不一致状态

### 10.1 服务配置

| 来源 | Key | 默认值 | 说明 |
|------|-----|--------|------|
| application.yml | `server.host` | `0.0.0.0` | 监听地址 |
| application.yml | `server.port` | `14325` | 监听端口 |
| 环境变量 | `SERVER_HOST` | — | 覆盖 host |
| 环境变量 | `SERVER_PORT` | — | 覆盖 port |
| 环境变量 | `CONFIG_PATH` | `application.yml` | 配置文件路径 |

### 10.2 数据路径

| 路径 | 说明 |
|------|------|
| `./data/library.db` | SQLite 数据库 |
| `./data/books/{id}/` | 每本书的独立目录 |
| `./data/books/{id}/book.{ext}` | 书籍文件 |
| `./data/books/{id}/cover.{ext}` | 封面图片 |

相对路径基于工作目录解析（`os.Getwd()`），回退到可执行文件目录。

### 10.3 开发模式

```
终端 1: cd backend && go run ./cmd/server        (端口 14325)
终端 2: cd client/web && npm run dev              (端口 5173, 代理 /api → 14325)
```

### 10.4 生产构建

```bash
# Windows
build.bat  →  backend/kh-e-library.exe

# Linux/macOS
./build.sh  →  kh-e-library-v0.0.3-linux-amd64.tar.gz
```

构建流程：前端 `npm run build` → 复制 `dist` 到 `backend/web/dist` → `CGO_ENABLED=0 go build`（嵌入前端）→ 输出单二进制。

### 10.5 Docker

```bash
# 从源码构建
docker build -f docker/Dockerfile.build -t e-library .
docker run -d -p 14325:14325 -v ./data:/app/data e-library

# 下载预编译二进制
docker build -f docker/Dockerfile.download -t e-library .
```

**docker-compose.yml 示例：**
```yaml
version: "3"
services:
  e-library:
    image: e-library
    ports:
      - "14325:14325"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
```

**健康检查说明：** Alpine 基础镜像不含 `curl`/`wget`。如需在 docker-compose 中添加 healthcheck，Dockerfile 需额外安装 `curl`（`apk add --no-cache curl`），然后使用 `curl -f http://localhost:14325/health` 作为探活命令。或通过外部监控系统直接探测 `/health` 端点。

**数据备份说明：** SQLite 数据库文件为 `./data/library.db`，备份时需确保无写入操作（建议使用 `sqlite3 library.db ".backup backup.db"` 在线备份），或直接停止服务后复制文件。

### 10.6 CI/CD

GitHub Actions，推送 `v*` tag 时触发：

| Job | 平台 | 产物 |
|-----|------|------|
| backend | linux/amd64, linux/arm64, windows/amd64 | `.tar.gz` / `.zip` |
| backend-darwin | darwin/amd64, darwin/arm64 | `.tar.gz` |
| android | — | `.apk`（条件构建，本阶段不规划设计） |
| release | — | GitHub Release + 所有产物 |

---

## 十一、已知问题与待改进项

### 11.1 功能缺陷

| 编号 | 问题 | 严重程度 | 说明 |
|------|------|---------|------|
| F-1 | 设置页多数选项未生效 | 高 | `ui.theme`、`ui.sort_field`、`ui.sort_order`、`ui.page_size`、`reader.font_size` 保存到数据库但前端未读取应用 |
| F-2 | 无暗色主题 | 中 | `ui.theme` 设置项存在但未实现 |
| F-3 | PDF 单页渲染 | 中 | 每次只渲染一页，无法连续滚动浏览 |
| F-4 | EPUB 字号不可调 | 低 | 设置中有 `reader.font_size` 但未应用到 epubjs rendition |
| F-5 | pdfjs worker 依赖 CDN | 中 | 离线环境无法使用 PDF 阅读器 |
| F-6 | 其他存储后端未实现 | 低 | 仅有接口定义，无具体实现 |
| F-7 | 搜索无防抖 | 低 | 每次输入都触发请求 |
| F-8 | 书架排序/分页未使用设置 | 中 | 硬编码 `updated_at DESC` 和 `pageSize=20` |
| F-9 | 缺少批量标签操作 | 低 | 为多本书添加同一标签需逐个调用 `/books/tags/add`，未来可增加批量接口 |

### 11.2 代码质量

| 编号 | 问题 | 严重程度 | 说明 |
|------|------|---------|------|
| Q-1 | 大量内联样式 | 中 | Reader.vue、BookShelf.vue 中大量 `style="..."` 内联样式，应抽取为 CSS class |
| Q-2 | 无组件拆分 | 中 | 三个 Vue 文件各自超过 100 行，无子组件拆分（如 BookCard、PDFControls） |
| Q-3 | `any` 类型滥用 | 低 | Reader.vue 中 `rendition`、`book`、`pdfDoc` 等均为 `any`，缺少类型安全 |
| Q-4 | 错误处理粗糙 | 低 | 多处 `catch { /* ignore */ }` 静默吞掉错误 |
| Q-5 | API 响应类型缺失 | 低 | `api/index.ts` 未使用泛型，`res.data.data` 为 `any` |
| Q-6 | 配置路径重复 | 低 | `backend/application.yml` 和 `backend/internal/config/application.yml` 内容完全相同 |

### 11.3 架构问题

| 编号 | 问题 | 严重程度 | 说明 |
|------|------|---------|------|
| A-1 | 全局 DB 变量 | 中 | `repository.DB` 为包级全局变量，不利于测试和未来扩展 |
| A-2 | Service 层过薄 | 低 | 大部分 Service 方法仅透传到 Repository，缺少业务逻辑抽象 |
| A-3 | Storage Factory 未使用 | 低 | `storage.Factory` 定义了 Register/Get/Cleanup 但未被任何代码调用 |
| A-4 | 封面路径使用绝对路径 | 中 | ~~`CoverPath` 存储的是服务器绝对路径~~ → 已改为 `CoverFile` 存储文件名，按 ID 目录组织 |
| A-5 | 设置热切换仅限存储 | 低 | `onSettings` 回调只处理 Storage Driver 切换，其他设置项变更无即时生效机制 |

### 11.4 安全问题

| 编号 | 问题 | 严重程度 | 说明 |
|------|------|---------|------|
| S-1 | 无认证鉴权 | 中 | 个人项目假定可信网络，但若暴露到公网则无任何保护 |
| S-2 | CORS 全开 | 低 | `Access-Control-Allow-Origin: *`，生产环境应限制来源 |
| S-3 | 文件上传无大小限制 | 中 | 可上传任意大小的文件，存在 DoS 风险 → 已设定 200MB 上限 |
| S-4 | 路径遍历未防护 | 中 | 上传文件名未做充分清洗，`filepath.Base` 仅取文件名但仍可能被绕过 |

---

## 十二、目录结构

```
kitakami_hibiki_epub_library/
├── .github/workflows/build.yml
├── .gitignore
├── README.md
├── VERSION                              # v0.0.3
├── build.bat                            # Windows 构建脚本
├── build.sh                             # Linux/macOS 构建脚本
├── backend/
│   ├── application.yml                  # 服务配置（host/port）
│   ├── go.mod / go.sum
│   ├── cmd/server/main.go               # 入口：路由注册、Storage 初始化
│   ├── internal/
│   │   ├── config/
│   │   │   ├── config.go                # 配置加载、路径解析
│   │   │   └── application.yml          # 默认配置（与根目录重复）
│   │   ├── epub/
│   │   │   └── parser.go                # EPUB ZIP 解析、OPF 元数据提取、封面提取
│   │   ├── handler/
│   │   │   ├── book_handler.go           # 书籍 CRUD + 上传 + 读取 + 封面
│   │   │   ├── reading_handler.go        # 进度
│   │   │   └── setting_handler.go        # 运行时设置
│   │   ├── middleware/
│   │   │   ├── cors.go                   # CORS 中间件
│   │   │   └── logger.go                 # 请求日志中间件
│   │   ├── model/
│   │   │   ├── book.go                   # Book 模型（CoverFile/BookFile 相对文件名，Tags gorm:"-"）
│   │   │   ├── tag.go                    # Tag 模型 + BookTag 关联模型
│   │   │   ├── reading_progress.go       # ReadingProgress 模型
│   │   │   └── setting.go               # Setting 模型 + 默认值常量
│   │   ├── repository/
│   │   │   ├── db.go                     # SQLite 初始化 + AutoMigrate
│   │   │   ├── book_repo.go             # Book/Progress CRUD
│   │   │   └── setting_repo.go          # Setting CRUD + BatchUpsert
│   │   ├── service/
│   │   │   └── book_service.go          # 业务逻辑层
│   │   └── storage/
│   │       ├── interface.go             # Driver 接口（以 bookID 为参数）
│   │       ├── local.go                 # 本地文件系统 Driver（按 ID 目录组织）
│   │       └── (其他 Driver 实现时添加)     # 其他存储后端 Driver
│   └── web/
│       └── web.go                       # embed.FS 静态资源 + SPA Fallback
├── client/web/
│   ├── package.json
│   ├── index.html
│   ├── vite.config.ts                   # Vite 配置（端口 5173，代理 /api）
│   ├── tsconfig.json / tsconfig.node.json
│   └── src/
│       ├── main.ts                      # Vue 应用入口 + i18n + Element Plus
│       ├── App.vue                      # 根组件（RouterView）
│       ├── api/index.ts                 # Axios API 客户端
│       ├── epubjs.d.ts                  # epubjs 类型声明
│       ├── router/index.ts              # Vue Router（3 条路由）
│       ├── types/book.ts               # TypeScript 接口（Book.tags: string[]）
│       ├── views/
│       │   ├── BookShelf.vue            # 书架页
│       │   ├── Reader.vue               # 阅读器页（EPUB + PDF）
│       │   └── Settings.vue             # 设置页
│       └── locales/
│           ├── index.ts                 # i18n 初始化 + localStorage 持久化
│           ├── zh-CN.ts                 # 简体中文
│           └── en.ts                    # 英文
└── docker/
    ├── Dockerfile.build                 # 多阶段构建
    └── Dockerfile.download              # 下载预编译二进制
```

---

## 十三、依赖清单

### 后端直接依赖

| 包 | 版本 | 用途 |
|----|------|------|
| `github.com/gin-gonic/gin` | v1.12.0 | HTTP 框架 |
| `github.com/glebarez/sqlite` | v1.11.0 | 纯 Go SQLite 驱动 |
| `gorm.io/gorm` | v1.31.2 | ORM |
| `github.com/pdfcpu/pdfcpu` | v0.13.0 | PDF 封面提取 |
| `gopkg.in/yaml.v3` | v3.0.1 | YAML 配置解析 |

### 前端直接依赖

| 包 | 版本 | 用途 |
|----|------|------|
| `vue` | ^3.5 | 前端框架 |
| `vue-router` | ^4.5 | 路由 |
| `vue-i18n` | ^10 | 国际化 |
| `element-plus` | ^2.9 | UI 组件库 |
| `@element-plus/icons-vue` | ^2.3 | 图标 |
| `axios` | ^1.7 | HTTP 客户端 |
| `epubjs` | ^0.3.93 | EPUB 阅读器 |
| `pdfjs-dist` | ^4.0.379 | PDF 阅读器 |

---

## 十四、重构建议

以下为重写项目时建议优先考虑的改进方向：

### 14.1 前端

1. **设置项生效**：书架页从 API 读取 `ui.page_size`、`ui.sort_field`、`ui.sort_order` 并应用；阅读器读取 `reader.font_size` 并应用到 epubjs rendition
2. **暗色主题**：实现 `ui.theme` 的 dark/auto 模式，使用 CSS 变量或 Element Plus 的 dark mode
3. **组件拆分**：抽取 `BookCard`、`PDFControls`、`ReaderToolbar` 等子组件
4. **样式规范**：消除内联样式，使用 scoped CSS + BEM 命名，统一 rem 单位
5. **类型安全**：为 epubjs/pdfjs 定义类型声明，API 响应使用泛型 `AxiosResponse<T>`
6. **PDF 连续滚动**：支持多页渲染或虚拟滚动
7. **pdfjs worker 本地化**：将 worker 文件打包到前端资源中，不依赖 CDN

### 14.2 后端

1. **依赖注入**：使用 `fx` 或手动构造，消除全局 `DB` 变量
2. **文件上传限制**：最大文件大小 200MB，超出返回 413 错误。通过 Gin 中间件 `MaxMultipartMemory`（内存缓冲上限 32MB）+ 自定义校验（检查 `Content-Length` 或读取文件大小）实现
3. **设置生效机制**：`onSettings` 回调应支持更多设置项的即时生效，而非仅 Storage Driver
4. **API 响应统一**：定义标准响应结构（code/message/data），错误码枚举
5. **清理重复配置**：移除 `backend/internal/config/application.yml`，仅保留根目录的

### 14.3 架构

1. **用户认证**：即使个人项目，至少添加 Basic Auth 或 Token 认证
3. **日志规范化**：使用结构化日志（如 `slog` 或 `zerolog`）替代 `log.Printf`
4. **配置验证**：启动时校验配置项的合法性，避免运行时 panic
